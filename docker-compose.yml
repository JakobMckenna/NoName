version: '3'

services:
  backend:
    image: noname4350/backend:latest
    env_file:
      - ./backend/.env
    ports:
      - "5001:5001"

  frontend:
    image: noname4350/frontend:newdb
    ports:
      - "3000:3000"
